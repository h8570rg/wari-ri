pre-commit:
  parallel: true
  jobs:
    - name: "Next.js Lint"
      run: "npm run lint -- --fix --file {staged_files}"
      glob: "*.{js,ts,jsx,tsx}"
      stage_fixed: true

    - name: "TypeScript Type Check"
      run: "npm run typecheck"
