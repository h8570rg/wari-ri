pre-commit:
  parallel: false
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: "pnpm lint -- --write {staged_files}"
      stage_fixed: true

    typecheck:
      run: "pnpm typecheck"
