pre-commit:
  parallel: false
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: "npm run lint -- --write {staged_files}"
      stage_fixed: true

    typecheck:
      run: "npm run typecheck"
